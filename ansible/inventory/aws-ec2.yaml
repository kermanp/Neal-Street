plugin: aws_ec2
regions:
  - us-east-1

keyed_groups:
  - key: tags.Environment
    prefix: env
  - key: tags.OS
    prefix: os

filters:
  tag:ManagedBy: Terraform
  instance-state-name: [ 'running' ]

hostnames:
  - ip-address